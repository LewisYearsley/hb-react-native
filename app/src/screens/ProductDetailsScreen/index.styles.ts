import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    padding: 8,
    fontSize: 28,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});

export default styles;
